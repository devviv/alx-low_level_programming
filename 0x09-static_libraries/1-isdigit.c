#include "main.h"
/**
* _isdigit - this program check if an integers is digit or not
*
* @c: character value
*
* Return: num
*/
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
