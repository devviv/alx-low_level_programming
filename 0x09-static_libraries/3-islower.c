#include "main.h"
/**
 * _islower - this program finds out if a letter is lowercase or not
 *
 * @c: letter value
 *
 * Return: num
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
