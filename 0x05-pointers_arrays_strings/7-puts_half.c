/**
 * puts_half - prints a half of string
 *@str: string
 * Return: nothing
 */
#include "main.h"
void puts_half(char *str)
{
int i, j, len;
i = 0;
char *new_string[100];
while (*(str + i) != '\0')
{
i++;
}
int n = i;
i = 0;
if (n % 2 == 0)
{
for (i = n / 2; i < n; i++)
{
_putchar()
}
}
}
